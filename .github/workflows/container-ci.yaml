# .github/workflows/container-ci.yml
name: Container CI

on:
  push:
    branches: [ p07-container-hardening ]
  pull_request:
    branches: [ main ]

jobs:
  container-checks:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Build Docker image
      run: |
        docker build -t fastapi-app .
        echo "✅ Image built successfully"

#    - name: Lint Dockerfile with config
#      run: |
#        docker run --rm -v ${PWD}/.hadolint.yaml:/.hadolint.yaml -i hadolint/hadolint < Dockerfile

    - name: Verify non-root user
      run: |
        user_id=$(docker run --rm fastapi-app id -u)
        echo "User ID: $user_id"
        if [ "$user_id" = "0" ]; then
          echo "❌ FAIL: Running as root"
          exit 1
        else
          echo "✅ PASS: Running as non-root user (ID: $user_id)"
        fi

    - name: Test container startup
      run: |
        docker run -d --name app-test -p 8000:8000 fastapi-app
        sleep 15
        health_status=$(docker inspect --format='{{.State.Health.Status}}' app-test)
        echo "Health status: $health_status"

        if [ "$health_status" = "healthy" ] || [ "$health_status" = "starting" ]; then
          echo "✅ PASS: Healthcheck working"
        else
          echo "❌ FAIL: Healthcheck not working"
          docker logs app-test
          exit 1
        fi

        docker stop app-test
        docker rm app-test
